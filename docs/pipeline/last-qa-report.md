## DoR: Diff Metadata
**Files:** 10 | **Added:** 23 | **Removed:** 17
**Functions modified:** none (docs/config only) | **New dependencies:** none

Diff scope: (1) Sherlock `maxTurns: 40 → 80` across three frontmatter/persona surfaces; (2) Sherlock Phase-2 narration guidance flipped from "narrate the hunt" to "do not narrate" across `source/shared/agents/sherlock.md` and installed `.claude/agents/sherlock.md`; (3) Eva's user-bug-flow step 3 changed from "relay the case file" to "read `docs/pipeline/last-case-file.md` from disk and present" across `source/shared/rules/default-persona.md` and installed `.claude/rules/default-persona.md`; (4) new Sherlock row in Eva's Receipt-Format table across `source/shared/rules/pipeline-orchestration.md` and installed `.claude/rules/pipeline-orchestration.md`; (5) user-guide state-files table gains `last-case-file.md` row.

## Exercised
- Grep-traced the wiring: `last-case-file.md` appears in source/shared + installed .claude consistently; also already present in Sherlock persona (`.claude/agents/sherlock.md:181`, installed) which instructs Sherlock to `Write the full case file to docs/pipeline/last-case-file.md`. So producer and consumer halves are wired.
- Verified Eva's tool surface (`.claude/rules/default-persona.md:70`): "Read files (Read, Glob, Grep)" is allowed everywhere. Eva reading `docs/pipeline/last-case-file.md` does not violate the write-scope gate at line 73 (which lists only write paths). The new step 3 is legal.
- Simulated step 3: attempted `ls docs/pipeline/` — `last-case-file.md` does not currently exist (no Sherlock runs post-change). The protocol doesn't define Eva's fallback if Sherlock returns the one-line receipt but the file is absent; in the happy path Sherlock's persona mandates the write, so this is guarded at the Sherlock end, not at Eva's.
- Verified `{pipeline_state_dir}` placeholder use across source/shared: `default-persona.md` and `agents/sherlock.md` use the placeholder consistently. The new row in `source/shared/rules/pipeline-orchestration.md:311` is the outlier — it hardcodes `docs/pipeline/last-case-file.md` (see Finding 1).
- Exercised the Cursor triple-target by grepping `.cursor-plugin/`: the plugin mirror still carries the OLD "Narrate the hunt" text (`.cursor-plugin/agents/sherlock.md:29`) and the OLD step-3 relay wording (`.cursor-plugin/rules/default-persona.mdc:116-118`). See Finding 2. Cursor `maxTurns` bump already landed at `source/cursor/agents/sherlock.frontmatter.yml:10` per the diff, but the Cursor plugin mirror (the installed target analogous to `.claude/`) is stale on the prose portions — the frontmatter overlay assembly won't carry the Phase-2 body edit because the body lives in `source/shared/agents/sherlock.md`, which was edited, yet the already-assembled `.cursor-plugin/agents/sherlock.md` wasn't re-generated.

## DoD: Verification
**Findings:** 2 | **Categories:** triple-target sync, template placeholder consistency, wiring | **Grep verified:** `last-case-file`, `pipeline_state_dir`, `Narrate the hunt`, `Sherlock.*verdict pinned`, `Case file` | **Exercised:** read-path simulation, tool-surface check against Eva's allow/deny lists, Cursor mirror grep

## Findings
| # | Location | Severity | Category | Description | Suggested Fix |
|---|----------|----------|----------|-------------|---------------|
| 1 | `source/shared/rules/pipeline-orchestration.md:311` | FIX-REQUIRED | template-consistency | The new Sherlock Receipt row hardcodes `docs/pipeline/last-case-file.md` while every other reference in `source/shared/` (including `source/shared/rules/default-persona.md:117` edited in this same diff and `source/shared/agents/sherlock.md:165,171,175`) uses the `{pipeline_state_dir}` placeholder. Projects that configure a non-default `pipeline_state_dir` at install time will get a receipt-format string pointing at `docs/pipeline/` while Sherlock actually writes to the configured dir — a cross-reference drift Eva will parrot into `pipeline-state.md`. | Replace `docs/pipeline/last-case-file.md` with `{pipeline_state_dir}/last-case-file.md` in the Sherlock row of the Receipt Format Per Agent table. (The installed `.claude/rules/pipeline-orchestration.md:315` is correct to resolve to `docs/pipeline/` since that mirror is already post-placeholder-substitution.) |
| 2 | `.cursor-plugin/agents/sherlock.md:29-32` and `.cursor-plugin/rules/default-persona.mdc:116-118` | FIX-REQUIRED | triple-target-sync | CLAUDE.md convention requires `source/`, `.claude/`, and `.cursor-plugin/` to stay in sync. The diff updates source and `.claude/` mirror but the `.cursor-plugin/` mirror is stale on the two prose edits: it still contains the old "Narrate the hunt the way a detective narrates a case" guidance (the exact text the diff was meant to suppress for narration-silencing) and the old step-3 "prepend 'Case file below.' and relay the case file as-is" without the "read from disk" step. Cursor users would get Sherlock narrating investigation steps — the opposite of the diff's intent. | Re-run the Cursor plugin assembly (or manually mirror the edits) so `.cursor-plugin/agents/sherlock.md` picks up the new Phase-2 "Do not narrate investigation steps…" paragraph from `source/shared/agents/sherlock.md:26-32`, and `.cursor-plugin/rules/default-persona.mdc:116-118` picks up the new step-3 "read `docs/pipeline/last-case-file.md` from disk" wording. Also confirm `.cursor-plugin/rules/pipeline-orchestration.mdc` gets the new Sherlock receipt row appended at line 310. |
