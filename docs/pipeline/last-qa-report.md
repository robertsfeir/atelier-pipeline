## DoR: Diff Metadata
**Files:** 9 (1 new module, 5 brain libs, 1 test file, 1 pipeline-config, 1 rules doc, 1 lockfile) | **Added:** ~830 | **Removed:** ~50
**Functions modified:** `classifyConflict`, `detectConflicts`, `getEmbedding`, `runConsolidation`, `synthesizeCluster`, `startConsolidationTimer`, `registerTools`, `registerAgentCapture`, `handleAgentCapture`. **New:** `embed`, `chat`, `chatOpenAICompat`, `chatAnthropic`, `verifyEmbeddingDimension`, `normalizeProviderConfig`, `buildAuthHeaders`, `buildProviderConfig`, `coerceProviderConfig`, `coerceChatProviderConfig`, `coerceConsolidationContext`.
**New dependencies:** none (pure refactor; new module is fetch-only).

## Exercised
- Ran `pytest tests/adr-0054/ -v` — 13/13 PASS (covers exports, openrouter back-compat, anthropic embed rejection, model_provider field, Bedrock/Vertex IDs, localhost base URL, no-auth-when-null, dimension mismatch + match, github-models headers).
- Mocked-fetch round-trip on the anthropic chat adapter: confirmed URL `/v1/messages`, `x-api-key` + `anthropic-version: 2023-06-01` headers, `system` lifted out of messages, `max_tokens: 4096` sent, response normalized to `choices[0].message.content` so existing `assertLlmContent` keeps working, and `JSON.parse` round-trips.
- Mocked-fetch null-config probe: `chat(messages, null)` and `embed(text, null)` both throw `providerConfig is required` (used to verify the stdio-mode path below).
- Greped for non-test callers of `verifyEmbeddingDimension` — zero hits across `skills/`, `scripts/`, `brain/lib/`, `brain/server.mjs`.
- Greped for residual `openrouter_api_key` references — all backward-compat shims line up except as flagged below.

## DoD: Verification
**Findings:** 3 | **Categories:** wiring, omission surface, backward-compat regression | **Grep verified:** `openrouter_api_key`, `OPENROUTER_API_KEY`, `verifyEmbeddingDimension`, `startConsolidationTimer`, `embedProviderConfig`, `chatProviderConfig` | **Exercised:** pytest suite, anthropic chat adapter, null-config error path, hydrate-script gating

## Findings
| # | Location | Severity | Category | Description | Suggested Fix |
|---|----------|----------|----------|-------------|---------------|
| 1 | `/Users/sfeirr/projects/atelier-pipeline-315e2ac4/brain/server.mjs:241` | FIX-REQUIRED | wiring (mode parity) | Stdio mode calls `startConsolidationTimer(pool, cfg.openrouter_api_key)` while HTTP mode (line 193) passes the new `{embedConfig, chatConfig}` bundle. For non-OpenRouter setups (e.g. user configured `embedding_provider: openai` + `openai_api_key` only), `cfg.openrouter_api_key` is `null`. `coerceConsolidationContext(null)` returns `{embedConfig: null, chatConfig: null}`, so every consolidation cycle calls `providerChat(messages, null)` and throws `providerConfig is required` — silently logged as `Consolidation LLM error for cluster: ...`. Stdio is the default mode (`process.argv[2] || "stdio"`, line 130), so this is the path most installs hit. Exercised the null-config branch via mocked fetch — confirmed both chat and embed throw on `null`. | Pass the same bundle as HTTP mode: `await startConsolidationTimer(pool, { embedConfig: cfg.embedProviderConfig, chatConfig: cfg.chatProviderConfig })`. The two startup paths should not diverge. |
| 2 | `/Users/sfeirr/projects/atelier-pipeline-315e2ac4/brain/lib/llm-provider.mjs:223-256` | FIX-REQUIRED | omission surface | `verifyEmbeddingDimension` is exported, fully tested, and documented as "the ADR-0054 dimension drift guard before any insert path runs", but no non-test code calls it. Greped `skills/brain-setup/`, `brain/scripts/`, `brain/server.mjs`, `brain/lib/*.mjs` — zero callers. The ADR's stated protection (catch a Gemini-shaped 768-dim vs schema 1536-dim mismatch before corrupting inserts) does not exist in the running system; only the unit test exercises the function. A user who points `embedding_provider` at a 768-dim model will silently corrupt every insert until pgvector rejects on dimension at INSERT time (and the error path there is generic, not the curated "run a schema migration" message this function produces). | Wire `verifyEmbeddingDimension(embedProviderConfig, 1536)` into either (a) `server.mjs` startup right after `buildProviderConfig`, exiting non-zero with the curated message on `ok: false`, or (b) the `brain-setup` skill flow before first insert. Without one of these, the guard is decorative. |
| 3 | `/Users/sfeirr/projects/atelier-pipeline-315e2ac4/brain/scripts/hydrate-telemetry.mjs:307,596` and `brain/scripts/hydrate-enforcement.mjs:84` | FIX-REQUIRED | backward-compat regression | Hydrate scripts still gate embedding generation on `config.openrouter_api_key` specifically, not on the new `embedProviderConfig`. A user who migrates to `embedding_provider: openai` + `openai_api_key` (and removes their old OpenRouter key) will trip the `if (config.openrouter_api_key)` branch as false and fall back to zero vectors — silent degradation, captured thoughts unsearchable. The migration is meant to be additive; this gate makes it subtractive for hydrate. | Either (a) replace the gate with `buildProviderConfig(config, 'embed')` + try/catch around it, passing the resulting providerConfig to `getEmbedding`, or (b) at minimum extend the gate to `if (config.openrouter_api_key || config.embedding_provider)` and pass the appropriate key. The second is a smaller change but still leaves the local-family case working without an apiKey. |
