## DoR: Diff Metadata
**Files:** 73 | **Added:** 1006 | **Removed:** 18819
**Functions modified:** N/A — bulk deletion of `brain/` (server.mjs, lib/*, migrations/*, schema.sql, ui/*, scripts/*, docker-compose.yml), all 22 files in `tests/brain/`, `skills/brain-setup/SKILL.md`, `.cursor-plugin/skills/brain-setup/SKILL.md`. Plugin manifests stripped of `mcpServers` + brain `SessionStart` hook entries. New: `tests/adr-0056/` (Brain Migration Wizard contract tests). Modified: `CLAUDE.md` (test_command + Tech Stack/Source Structure sections), `skills/pipeline-setup/SKILL.md` + cursor mirror (Step 0f migration wizard added; brain-setup references replaced with mybrain references).
**New dependencies:** none

## Exercised
- `pytest tests/` — 413 passed, 1 warning, 8.59s. Test command in CLAUDE.md matches reality.
- `bash .claude/hooks/session-hydrate-enforcement.sh` — exit 0 (fail-open path triggers because `$PROJECT_DIR/brain/scripts/hydrate-enforcement.mjs` is now absent). Hook is a no-op every session; not crashing, but doing nothing.
- `bash -c 'cd brain && node --test ../tests/brain/*.test.mjs'` (the test_command still in `source/claude/hooks/enforcement-config.json`) — fails immediately: `bash: cd: brain: No such file or directory`.
- Grep-verified: `${CLAUDE_PLUGIN_ROOT}/brain/server.mjs` removed from `.claude-plugin/plugin.json`. `${CURSOR_PLUGIN_ROOT}/brain/server.mjs` is STILL referenced in `.cursor-plugin/mcp.json` (sibling of `plugin.json`, loaded by Cursor at plugin init).
- Grep-verified: `enforce-brain-capture-gate.sh`, `prompt-brain-prefetch.sh`, `clear-brain-capture-pending.sh`, `enforce-brain-capture-pending.sh` all still exist in both `.claude/hooks/` and `source/claude/hooks/`. `.claude/settings.json` still wires them and still pre-approves the 8 `mcp__plugin_atelier-pipeline_atelier-brain__*` tool names. Per ADR-0055 wording in pipeline-setup SKILL.md these hooks/permissions remain by design (mybrain plugin re-registers tools under a new prefix; old prefixes are stripped by Step 0e on next setup run). Acceptable given the migration story.

## DoD: Verification
**Findings:** 3 (1 BLOCKER, 2 FIX-REQUIRED) | **Categories:** dead refs, cross-layer wiring, omission surface, integration surface | **Grep verified:** `brain/server.mjs`, `brain/scripts/`, `hydrate-enforcement.mjs`, `atelier-brain`, `${CURSOR_PLUGIN_ROOT}/brain`, `test_command`, `cd brain && node --test` | **Exercised:** pytest full suite, session-hydrate-enforcement.sh, the stale test_command shell snippet, plugin manifests, .cursor-plugin/mcp.json read

## Findings
| # | Location | Severity | Category | Description | Suggested Fix |
|---|----------|----------|----------|-------------|---------------|
| 1 | `.cursor-plugin/mcp.json` | BLOCKER | cross-layer wiring / dead ref | This file still registers the MCP server `atelier-brain` with `args: ["${CURSOR_PLUGIN_ROOT}/brain/server.mjs"]` and the same env block (`ATELIER_BRAIN_DB_PASSWORD`, `OPENROUTER_API_KEY`, `NODE_TLS_REJECT_UNAUTHORIZED=0`) that was deliberately removed from `.claude-plugin/plugin.json`. The Cursor plugin layout uses `mcp.json` as a sibling of `plugin.json`/`hooks.json`/`marketplace.json` (loaded automatically at plugin init), so on next session start Cursor will spawn `node ${CURSOR_PLUGIN_ROOT}/brain/server.mjs` against a path that no longer exists — MCP init failure on every Cursor user upgrading to this version. The Claude side of the brain extraction was completed; the Cursor side was missed. | Delete `.cursor-plugin/mcp.json` (the Claude analogue, the `mcpServers` block, was deleted entirely from `.claude-plugin/plugin.json` rather than emptied — match that). Same goes for `source/cursor/` if a template form exists. Keep the new mybrain plugin to reintroduce Cursor MCP wiring on its own terms. |
| 2 | `source/claude/hooks/session-hydrate-enforcement.sh` (and installed copy in `.claude/hooks/`) | FIX-REQUIRED | dead ref / omission surface | Hook still resolves `HYDRATE_SCRIPT="$PROJECT_DIR/brain/scripts/hydrate-enforcement.mjs"`. That script was deleted in this diff (it lived in `brain/scripts/`, which is gone). Behavior: the file-existence guard (`[ -f "$HYDRATE_SCRIPT" ] || exit 0`) makes the hook fail-open silently — no crash, but the hook is now a permanent no-op fired on every SessionStart, and `.claude/settings.json` still registers it (line 85). Verified by running it: exit 0, did nothing. The brain-extraction diff removed the script's destination but not the registered consumer. ADR-0031 enforcement-audit hydration is silently disabled. Either delete the hook + its registration in `source/claude/settings.json.template` (and `.claude/settings.json`), or move the script to mybrain and update the path. Documentation does not say which. | Decide whether ADR-0031 enforcement bulk-capture stays. If yes, the script must move to the mybrain plugin and the hook needs to call it from there. If no, delete `session-hydrate-enforcement.sh` and remove the SessionStart entry from `.claude/settings.json` line 85. Currently you have a registered hook that does nothing, which is the worst of both worlds — it looks active but is silently disabled. |
| 3 | `source/claude/hooks/enforcement-config.json` (and installed `.claude/hooks/enforcement-config.json`) | FIX-REQUIRED | stale config / omission surface | `"test_command": "pytest tests/hooks/ && cd brain && node --test ../tests/brain/*.test.mjs"`. Both halves are now broken: `tests/brain/` is deleted (22 test files gone) and `brain/` is deleted (cd target absent). Exercising it: `cd: brain: No such file or directory`. CLAUDE.md was updated to `pytest tests/` and that is the value Eva interpolates as `{test_command}` per the orchestration rules (placeholder is sourced from CLAUDE.md's "Test Commands" section, not from this JSON). However, this JSON is the literal default copied to user projects by `pipeline-setup` and serves as documentation of the canonical test command — it now embeds a guaranteed-to-fail command. Same for `source/shared/references/invocation-templates.md` line 4 (CONFIGURE comment example uses the same stale snippet). | Update `test_command` in `source/claude/hooks/enforcement-config.json` and `.claude/hooks/enforcement-config.json` to `"pytest tests/"` to match CLAUDE.md. Update the CONFIGURE example in `source/shared/references/invocation-templates.md` and its `.claude/` mirror. |
