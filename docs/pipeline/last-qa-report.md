## DoR: Diff Metadata
**Files:** 9 (3 source/claude hooks + 3 .claude installed mirrors + .claude/settings.json + 2 SKILL.md + 1 test addition + 2 state files) | **Added:** ~115 | **Removed:** ~15
**Functions/scripts modified:** `clear-brain-capture-pending.sh` (suffix-match on `__agent_capture` instead of settings.json `matcher` field), `enforce-brain-capture-gate.sh` (new `.brain-not-installed` sentinel honored alongside `.brain-unavailable`), `enforce-brain-capture-pending.sh` (short-circuit when either sentinel present), `.claude/settings.json` (PostToolUse `matcher` field removed), `skills/pipeline-setup/SKILL.md` (new Step 0e migration cleanup), `skills/brain-setup/SKILL.md` (ADR-0055 dedup of stale entries), `.cursor-plugin/skills/pipeline-setup/SKILL.md` (Step 1d only — partial sync), `tests/adr-0054/test_adr_0054.py` (Step 1d assertions, file truncates at line 600 of diff).
**New dependencies:** none — pure decoupling refactor. `jq` requirement on the clear hook elevated from "best-effort config read" to "fail-open if missing" because tool_name parsing now needs it.

## Exercised
- **Reconstructed the test rig in `/tmp/poirot-test`** with a project-root layout (`docs/pipeline/.pending-brain-capture.json`) and ran `clear-brain-capture-pending.sh` against four payloads:
  1. `{"tool_name":"mcp__plugin_atelier-pipeline_atelier-brain__agent_capture"}` -> pending file deleted (legacy prefix preserved). PASS.
  2. `{"tool_name":"mcp__custom__agent_capture"}` -> pending file deleted (alternate plugin prefix; suffix-match works). PASS.
  3. `{"tool_name":"Read"}` -> pending file untouched (correct no-op). PASS.
  4. `{"foo":"bar"}` (no tool_name) -> pending file untouched (correct no-op). PASS.
  5. `{"tool_name":"my_agent_capture"}` (no `__` separator) -> pending file untouched (correctly requires the namespace separator). PASS.
  6. `{"tool_name":"__agent_capture"}` (bare suffix) -> pending file deleted (matches `*__agent_capture`). Acceptable — no realistic adversary surface here.
- **Exercised `enforce-brain-capture-gate.sh`** with three states (pending file always present, `agent_id` empty so main-thread enforcement applies):
  1. No sentinel -> exit 2 with BLOCKED message naming `colby` from the pending JSON. PASS — message text now reads "Call agent_capture (via your brain plugin)" and references both `.brain-unavailable` and `.brain-not-installed` paths in the escape-hatch documentation.
  2. `.brain-not-installed` present -> exit 0 (pass-through). PASS.
  3. `.brain-unavailable` present -> exit 0 (pass-through, regression-safe). PASS.
- **Ran `python3 -m pytest tests/adr-0055/test_adr_0055.py -v`** -> 6/6 PASS in 0.20s, covering both clear-hook suffix behaviors, both gate sentinel behaviors, and both pending-hook short-circuit behaviors. The new tests bind to the actual installed hooks under `.claude/hooks/`, not source.
- **Inspected `.claude/settings.json`** directly: PostToolUse entry for `clear-brain-capture-pending.sh` no longer has a `matcher` key. The hook now self-filters via `case "$TOOL_NAME" in *__agent_capture)` — verified.
- **Verified source/installed parity** for the three modified hooks via `diff` (.claude/hooks/ matches source/claude/hooks/ byte-for-byte). Triple-target convention upheld for hooks.

## DoD: Verification
**Findings:** 2 (1 FIX-REQUIRED, 1 NIT) | **Categories:** dual-target sync, gate UX | **Grep verified:** `__agent_capture`, `mcp__plugin_atelier-pipeline_atelier-brain__agent_capture` (zero residual hardcoded references in hook bodies), `.brain-not-installed`, `Step 0e`, `stale_prefix`, `cursor-plugin/skills` | **Exercised:** clear hook against 6 payload variants; gate hook against 3 sentinel states; ADR-0055 pytest suite (6/6 PASS); settings.json matcher removal; source/installed hook parity diff.

## Findings
| # | Location | Severity | Category | Description | Suggested Fix |
|---|----------|----------|----------|-------------|---------------|
| 1 | `.cursor-plugin/skills/pipeline-setup/SKILL.md` (missing Step 0e block, present at line 148 in `skills/pipeline-setup/SKILL.md`) and `.cursor-plugin/skills/brain-setup/SKILL.md` (no ADR-0055 references; the Claude variant `skills/brain-setup/SKILL.md` has the dedup snippet at lines 147 and 174-183) | FIX-REQUIRED | dual-target sync | The Cursor variants of both setup skills are partially synced. Pipeline-setup got the Step 1d (Model Provider Selection) addition but did NOT receive Step 0e (Migrate Stale Brain MCP `permissions.allow` Entries — the Phase 1 migration cleanup). Brain-setup got nothing — no ADR-0055 migration note, no `stale_prefix` dedup logic. CLAUDE.md says "Triple target ... All stay in sync within their respective contexts" and ADR-0055's stated value is to migrate stale entries before they become dead weight. Cursor users running `/pipeline-setup` or `/brain-setup` post-rollout will retain stale `mcp__plugin_atelier-pipeline_atelier-brain__*` permission entries forever — exactly the "dead weight" the migration was written to prevent. The drift is invisible to CI: the new tests in `tests/adr-0054/test_adr_0054.py` only assert against `PROJECT_ROOT / "skills" / "pipeline-setup" / "SKILL.md"`, never the cursor-plugin path. Confirmed via `grep -n "Step 0e\|ADR-0055\|stale_prefix" .cursor-plugin/skills/pipeline-setup/SKILL.md` -> zero hits; same grep against `skills/pipeline-setup/SKILL.md` -> 7 hits. | (a) Port Step 0e and the brain-setup ADR-0055 dedup snippet into the two `.cursor-plugin/skills/.../SKILL.md` files in this commit; AND (b) parametrize at least one of the new SKILL.md tests to also assert against the cursor-plugin variant so the next round of drift is caught at CI. (a) closes the immediate gap; (b) is the durable safety net — without it, this dual-target drift is statistically guaranteed to recur. |
| 2 | `source/claude/hooks/enforce-brain-capture-gate.sh:71-73` (and the installed mirror at `.claude/hooks/enforce-brain-capture-gate.sh:71-73`) | NIT | gate UX | The BLOCKED message now reads "Call agent_capture (via your brain plugin)" — accurate, but no longer tells Eva the actual tool name to call. Pre-ADR-0055 the message named the exact MCP tool ID; post-decoupling, "via your brain plugin" leaves Eva to guess between e.g. `mcp__plugin_atelier-pipeline_atelier-brain__agent_capture` (current) and `mcp__plugin_mybrain__agent_capture` (Phase 2/3). When the gate fires Eva is exactly the agent who needs the prefixed name to invoke the tool — the suffix-match is a hook-side concern, not an Eva-side one. Exercised: the gate fires correctly (verified above), the message is grammatically clean and the escape-hatch docs are accurate; this is purely a discoverability nit on the prefixed tool name. | Append one line: "Use whichever `*__agent_capture` tool your installed brain plugin registered (run `/mcp` to list available tools if unsure)." This restores the discoverability path without re-coupling the message to a single plugin name. |

